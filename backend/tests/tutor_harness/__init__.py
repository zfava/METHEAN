"""Permanent quality harness for the assembled tutor context.

This package proves the tutor context METHEAN hands the model is always
correct, complete, bounded, and clean across every combination of
autonomy policy, developmental register, live signal, and relationship
memory state. It asserts on the ASSEMBLED CONTEXT (the exact text the
model would receive), never on model output, and makes zero AI calls.
"""
